[package]
name = "parser"
version = "0.9.1"
description = "卡牌语法解析器 — 词法/语法分析、语法校验、卡牌数据管理"
authors = ["Obwiler"]
edition = "2021"

[dependencies]
core = { path = "../core", package = "cardmaker-core" }
serde.workspace = true
serde_json.workspace = true
